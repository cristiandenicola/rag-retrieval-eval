[project]
name = "rag-eval"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0"
]

[project.scripts]
rag-eval = "rag_eval.run:main"